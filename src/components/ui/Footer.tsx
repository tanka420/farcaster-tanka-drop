"use client";

import React from "react";

// Footer hiện tại không dùng nữa vì Boss đã bỏ hệ thống tabs.
// Để tránh lỗi build, ta trả về null (không render gì).

export function Footer() {
  return null;
}

export default Footer;
